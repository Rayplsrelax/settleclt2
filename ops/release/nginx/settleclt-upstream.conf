# Include this block once in nginx's http context. The included path is
# an operator-managed symlink to blue.conf or green.conf.
upstream settleclt_app {
    include /etc/nginx/settleclt-active-upstream.conf;
    keepalive 32;
}